import React, { Fragment } from 'react';
import RoleList from 'src/components/Settings/Roles/list';

const Main = () => {
    return (
        <Fragment>
            <RoleList />
        </Fragment>
    );
};

export default Main;